package org.ipring;

/**
 * @Author lgj
 * @Date 2024/6/29
 */
public class ActivitiUserTest {
    /*private ProcessEngine defaultProcessEngine = ProcessEngines.getDefaultProcessEngine();

    *//**
     * 设置流程节点办理人
     * @param processName
     *//*
    public void startProcessInstance2(String processName) {
        RuntimeService runtimeService = defaultProcessEngine.getRuntimeService();
        // 指定当前节点办理人为张三
        User user = new User();
        user.setUserID("zhangShan");
        ProcessInstance processInstance =
                runtimeService.startProcessInstanceByKey(processName, JsonUtils.toMap(user));
        System.out.println("---->>" + processInstance.getId());
        System.out.println("---->>" + processInstance.getProcessDefinitionName());
        System.out.println("---->>" + processInstance.getProcessDefinitionId());
        System.out.println("---->>" + processInstance.getBusinessKey());
    }*/
}










